image: python:3.13

stages:
  - lint

lint:
  stage: lint
  script:
    - pip install --no-cache-dir -r requirements.txt
    - pylint / | tee pylint
  artifacts:
    paths:
      - pylint
    when: always
    expire_in: 1 week
  rules:
    - when: always